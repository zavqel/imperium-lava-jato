const box=document.querySelector('#appointment-summary');const data=JSON.parse(localStorage.getItem('imperiumAtendimento')||'null');if(data){box.innerHTML=data.items.map(i=>`<p>${i.nome}<br><strong>${money(i.preco)}</strong></p>`).join('')+`<hr><h2>${money(data.total)}</h2><a class="btn btn-primary full" href="https://wa.me/5516999575780?text=Olá!%20Quero%20agendar%20este%20atendimento:%20${encodeURIComponent(data.items.map(i=>i.nome).join(', '))}" target="_blank">Continuar pelo WhatsApp</a>`;}else{box.innerHTML='<p>Nenhum atendimento foi montado.</p><a class="btn btn-primary" href="montar.html">Montar meu atendimento</a>';}
